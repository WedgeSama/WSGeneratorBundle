{#########################################################################
 # This file is part of the WSGeneratorBundle package.
 # 
 # (c) Benjamin Georgeault <https://github.com/WedgeSama/>
 # 
 # For the full copyright and license information, please view the LICENSE
 # file that was distributed with this source code.
 #########################################################################}
{% set opts = {'name': field.columnName, 'type': field.type} %}
{% if field.type_opts is defined %}
    {%- set opts = opts|merge(field.type_opts) %}
{%- endif %}

    /**
     * @var {{ field.type }}
     *
     * @ORM\Column({% for opt_name, opt_value in opts %}{{ opt_name }}="{{ opt_value }}"{% if not loop.last %}, {% endif %}{% endfor %})
     {% if field.id is defined -%}
     * @ORM\Id
     * @ORM\GeneratedValue(strategy="AUTO")
     {%- endif %}
     {%- if field.validate_rules is defined -%}
     {%- for valid_rules in field.validate_rules -%}
     * @Assert\{{ valid_rules.name }}({% for opt in valid_rules.opts %}{{ opt.name }}={% if opt.value is defined %}"{{ opt.value }}"{% elseif opt.values is defined %}{% for param in opt.values %}{% if loop.first %}{{ "{" }}{% endif %}"{{ param }}"{% if loop.last %}{{ "}" }}{% else %}, {% endif %}{% endfor %}{% endif %}{% if not loop.last %}, {% endif %}{% endfor %})
     {% endfor -%}
     {%- endif -%}
     */
    private ${{ field.fieldName }};
